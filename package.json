{
  "name": "typesafe-as-a-judge",
  "version": "0.1.0",
  "description": "A dual Codex and Claude MCP plugin for bounded TypeSafe Jev judgments.",
  "type": "module",
  "private": true,
  "engines": {
    "node": ">=20"
  },
  "scripts": {
    "start": "node server/index.mjs",
    "test": "node --test tests/**/*.test.mjs"
  }
}
